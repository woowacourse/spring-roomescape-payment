spring:
  profiles:
    active: test

---

spring:
  datasource:
    # h2 서버 url
    # url: jdbc:h2:mem:testtest;DATABASE_TO_LOWER=TRUE
    # h2 embedded url
    url: jdbc:h2:~/testtest;AUTO_SERVER=TRUE;DATABASE_TO_LOWER=TRUE
    username: sa

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true
      path: /h2-console
  config:
    activate:
      on-profile: test

jwt:
  secret-key: gotjrgoeh2rjtdmsqufakfdldkslqslekgoehrgkwlaktpdy=
  expiration: 1800000
