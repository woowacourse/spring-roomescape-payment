spring:
  profiles:
    active: test

---

spring:
  datasource:
    # h2 서버 url
    # url: jdbc:h2:mem:test-test;DATABASE_TO_LOWER=TRUE
    # h2 embedded url
    url: jdbc:h2:mem:test-test;DATABASE_TO_LOWER=TRUE
    username: sa

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: false
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true
      path: /h2-console
  config:
    activate:
      on-profile: test

jwt:
  secret-key: gotjrgoeh2rjtdmsqufakfdldkslqslekgoehrgkwlaktpdy=
  expiration: 1800000

payment:
  toss:
    base-url: https://api.tosspayments.com
    confirm-url: /v1/payments/confirm
    widget-secret-key: test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
