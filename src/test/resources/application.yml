spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:database
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true

security:
  jwt:
    token:
      secret-key: pumExiFixehC65FymaHwN/FDue8U48AO
      expire-length: 3600000

payment:
  toss:
    secret: test
    base-uri: /confirm
    confirm-uri: doesnotmatter
