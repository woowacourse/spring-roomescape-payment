spring:
  sql:
    init:
      mode: never
  datasource:
    url: jdbc:h2:mem:database-test
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    open-in-view: false
