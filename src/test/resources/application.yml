spring:
  datasource:
    url: jdbc:h2:mem:database
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: never

roomescape:
  payment:
    confirm-uri: &confirm-uri /confirm
  rest-client:
    connect-timeout: &connect-timeout 30s
    read-timeout: &read-timeout 30s
  security:
    expiration: 3600s
    secret: pumExiFixehC65FymaHwN/FDue8U48AO

payment:
  toss:
    base-uri: http://localhost
    confirm-uri: *confirm-uri
    connect-timeout: *connect-timeout
    read-timeout: *read-timeout
    secret: test
