[source,bash]
----
$ curl 'http://docs.api.com/members' -i -X POST \
    -H 'Content-Type: application/json' \
    -H 'Accept: application/json' \
    -d '{
  "email" : "example@example.com",
  "password" : "password",
  "name" : "홍길동"
}'
----