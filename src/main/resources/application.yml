spring:
  profiles:
    active: local # 기본 값 local, 운영 시에 prod로 변경

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:database

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

toss:
  payments:
    secret-key: test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
    url: https://api.tosspayments.com/v1/payments/
