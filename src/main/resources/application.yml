spring:
  datasource:
    url: jdbc:h2:mem:database
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: none
security:
  jwt:
    token:
      secret-key: secretkeyofspringroomescapewaitingmissionstep1
toss:
  payments:
    base-url: https://api.tosspayments.com
    api:
      confirm-url: /v1/payments/confirm
      refund-url-template: /v1/payments/{paymentKey}/cancel
    widget-secret-key: test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
